Place patient loop videos here:

- `idle.mp4` — breathing loop (plays on load, loops)
- `deterioration.mp4` — death/deterioration sequence (plays once on timer expiry)

Both should be 16:9 or similar; they are scaled to full-bleed inside the scene stage.
